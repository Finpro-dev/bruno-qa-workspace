info:
  name: count-cart
  type: http
  seq: 4

http:
  method: GET
  url: "{{BASE_URL}}/cart/count"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
