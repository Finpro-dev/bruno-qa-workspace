info:
  name: set primary store
  type: http
  seq: 9

http:
  method: PATCH
  url: "{{BASE_URL}}/transactions"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
