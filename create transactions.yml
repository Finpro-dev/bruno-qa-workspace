info:
  name: create transactions
  type: http
  seq: 4

http:
  method: POST
  url: "{{BASE_URL}}/transactions"
  body:
    type: json
    data: |-
      {
        "addressId" : "ec31ede2-d31b-48b8-8a7b-823d740ed70f"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
