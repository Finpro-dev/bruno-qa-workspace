info:
  name: create store
  type: http
  seq: 11

http:
  method: POST
  url: "{{BASE_URL}}/stores"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
