info:
  name: get primary store
  type: http
  seq: 14

http:
  method: GET
  url: "{{BASE_URL}}/stores/me/primary-store"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
